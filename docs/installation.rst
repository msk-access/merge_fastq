.. highlight:: shell

============
Installation
============


Stable release
--------------

To install merge_fastq, run this command in your terminal:

.. code-block:: console

    $ pip install merge_fastq

This is the preferred method to install merge_fastq, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for merge_fastq can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/rhshah/merge_fastq

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/rhshah/merge_fastq/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/rhshah/merge_fastq
.. _tarball: https://github.com/rhshah/merge_fastq/tarball/master
